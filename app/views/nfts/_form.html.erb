<%= simple_form_for @nft do |f| %>
  <%= f.input :photo, as: :file %>
  <%= f.input :name %>
  <%= f.input :description %>
  <%= f.input :price, input_html: { min: 1 } %>
  <%= f.input :availability, label: "Is it currently available?" %>
  <%= f.button :submit %>
<% end %>
