<div class="container">
  <h1>NFTS</h1>

<div class="selection border rounded-4 mb-5 p-3">
<h2>Sélection du mois</h2>
  <div class="list-group d-flex flex-row d-inline-flex w-100">
    <% @nfts.where(availability: true).sample(3).each do |nft| %>
      <div class="nft border rounded mx-3">
        <div class="item mt-3">Name: <%= nft.name %></div>
        <div class="item">Description: <%= nft.description %></div>
        <div class="item">Price: <%= nft.price %></div>
        <div class="item">Availability: <%= nft.availability %></div>
        <div class="item">ID: <%= nft.user_id %></div>
      </div>
    <% end %>
  </div>
</div>

<div class="list-group d-flex flex-row flex-wrap d-inline-flex w-100">
    <% @nfts.each do |nft| %>
      <div class="nft border rounded w-25 mx-3">
        <%= link_to nft_path(nft) do %>
          <div class="item mt-3">Name: <%= nft.name %></div>
        <% end %>
          <div class="item">Description: <%= nft.description %></div>
          <div class="item">Price: <%= nft.price %></div>

          <div class="item">Availability: <%= nft.availability %></div>
          <div class="item">ID: <%= nft.user_id %></div>
        </div>
    <% end %>
  </div>
</div>
